# content

公開される記事だけを置く。1 記事 1 ディレクトリで `<slug>/index.md`、画像は同じ
ディレクトリに置いて相対パスで参照する。形式は `../CONTRACT.md` が正本。

E2E が使うフィクスチャはここではなく `../test-content/posts/` にある。**テスト用の
記事をここに置かないこと。** 混ぜると、記事を書き換えるたびにテストが落ちるようになる。

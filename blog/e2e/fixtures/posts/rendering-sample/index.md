---
title: 描画サンプル
date: 2026-08-23
tags:
  - fixture
  - sample
description: Markdown が期待どおり描画されるかを検査するための固定物。公開されない。
public_id: 00000000-0000-4000-8000-000000000001
---

このファイルは **E2E のフィクスチャ**であって記事ではない。`e2e/blog.spec.ts`
の「記事の描画」「コードハイライト」の節がここの中身に依存しているので、書き換える
ときはテストも一緒に直すこと。

## 見出し 2

段落と `inline code` とリンク（[CONTRACT.md](../../../CONTRACT.md)）。

### 見出し 3

1. 番号付きリスト
2. の 2 行目

- 箇条書き
- の 2 行目

## コードブロック

`export` を含む行があること。ハイライトの検査がこの語を目印にしている。

```ts
export function displayDate(d: Date): string {
  return d.toISOString().slice(0, 10).replaceAll('-', '/');
}
```

## 画像

記事と同じディレクトリに置いた画像を相対パスで参照する。

![サンプル画像](./sample.png)

EXIF の回転情報を持つ写真。**格納値は 96x48 だが、ブラウザは 48x96 として描く。**
寸法をヘッダから読むときにここを見落とすと、横長の枠を予約してから縦長で描き直す
ことになる。

![回転情報つきの写真](./rotated.jpg)

## 本文の中の HTML

本文に書いた HTML は、**どこにも書き換えられずにそのまま出る**こと。
インラインの `<img src="./dog.png">` と、言語指定なしのコードブロック:

```
<img src="./cat.png">
```

## 引用と区切り

> 引用も使える。

---

| 表 | も |
|---|---|
| 使 | える |

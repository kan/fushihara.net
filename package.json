{
  "name": "fushihara-net",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev -- vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=fushihara-net"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "wrangler": "^4.65.0"
  },
  "dependencies": {
    "@kanf/wema": "^0.3.1",
    "simple-icons": "^16.9.0"
  }
}
